version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: evento-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: evento_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: evento-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
